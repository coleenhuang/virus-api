CREATE TABLE virus_article(
  article_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  article TEXT NOT NULL,
  date_posted TIMESTAMPTZ DEFAULT now() NOT NULL,
  virus_id INTEGER
        REFERENCES virus(virus_id) ON DELETE CASCADE NOT NULL
);
