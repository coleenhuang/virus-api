CREATE TABLE viruses (
  virus_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  virus_name TEXT UNIQUE NOT NULL,
  family INTEGER
    REFERENCES classification(family_id) ON DELETE CASCADE NOT NULL
);
